#pragma once

/*
 * Copiá este archivo como secrets.h (misma carpeta) y completá tus valores.
 * secrets.h está en .gitignore y no se sube al repositorio.
 *
 * DEVICE_TOKEN debe coincidir con ESP32_DEVICE_TOKEN en server/.env
 */

constexpr const char* WIFI_SSID = "TU_WIFI";
constexpr const char* WIFI_PASSWORD = "TU_PASSWORD";
// IPv4 de tu PC (ipconfig) + :3001 en local, o URL de Render en producción (sin barra final)
constexpr const char* API_BASE_URL = "http://192.168.1.100:3001";
constexpr const char* DEVICE_TOKEN = "dev-esp32-token";
